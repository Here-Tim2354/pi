
# TLDR

1. 新增`@tim2354/pi-limit`的余额查询packages

# 余额查询

**背景**：这还需要背景？谁想每次都上官网或者官方软件查看内容？

**设计**：不过这一次我把之前的Opencode的查询也整合了进来，通过`/limit <parameters>`传参的方式来支持更多的提供商。目前覆盖我持有的订阅。

包也符合Pi社区规范：
```
  limit/
    index.ts          /limit 命令、参数分发、entry renderer 注
册
    types.ts          LimitReport / LimitWindow 模型（与厂商无
关）
    render.ts         报告 → TUI 文本（唯一管排版的地方）
    credentials.ts    走 pi 的凭据解析
    util.ts           JSON 防御式取值、额度换算、fetch、错误映
射
    providers/        codex.ts kimi.ts opencode.ts zai.ts
    package.json      @tim2354/pi-limit
    README.md
```
