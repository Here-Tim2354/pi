提交：`d9fb6d633`

# TLDR

1. `npm run sync` 重新纳入扩展同步：项目扩展安装到 `~/.pi/agent/extensions/`
2. 删除 `analyze-image.ts`，视觉子代理退出仓库扩展集

## 1-扩展重回 sync

**背景**：08-19 回撤后扩展没有全局安装渠道，但全局体验又确实需要它们。

**决策**：折中方案——扩展只保留一份在仓库，sync 复制到全局，全局生效。

## 2-遗留隐患

**背景**：仓库内 `.pi/extensions/` 仍会被 pi 自动发现，与全局副本双重注册的风险并没有消除，只是靠"记得别在仓库内重复加载"的约定维持。

**设计**：约定不可靠，靠改名解决。这个隐患直接催生了 09-01 的 `extensions-src` 迁移，见该日文档。

## 附

`analyze-image` 若要恢复，从 `5bbdc2239` 取回。
